import styles from "./sectionContacts.module.scss";

export default function SectionContacts() {
    return (
        <section className={styles.sectionContacts}>
            <div className={styles.sectionContacts__container + " _container"}>
                
            </div>
        </section>
    )
}